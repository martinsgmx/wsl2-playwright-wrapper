{
  "name": "playwright-wrapper-mcp",
  "version": "0.2.5",
  "type": "module",
  "description": "playwright-wrapper-mcp — Playwright MCP wrapper (WSL opencode → Windows Brave/Chrome via CDP) — isolated sessions + auth until redirect",
  "scripts": {
    "setup": "bash scripts/setup-mcp.sh",
    "check:cdp": "bash scripts/check-cdp.sh",
    "smoke": "bash scripts/smoke-test.sh",
    "test:isolated": "bash scripts/test-isolated.sh",
    "test:auth": "bash scripts/test-auth.sh",
    "capture:state": "bash scripts/capture-storage-state.sh",
    "login:server": "node test/fixtures/login/server.js"
  },
  "devDependencies": {
    "playwright": "^1.55.0"
  }
}
